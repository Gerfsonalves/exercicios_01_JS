const paises = ['Alemanha', 'Argentina', 'Brasil', 'México']

// Percorre item por item
paises.forEach(Element => {
    console.log('Execução número: ', Element)
})

// ----------------------------------------------------------

for (let index = 0; index < paises.length; index++) {
    console.log(paises[index])
}